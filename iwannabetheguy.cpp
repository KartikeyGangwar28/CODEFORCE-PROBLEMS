#include <iostream>
#include <vector>
using namespace std;
int main(){
    int n;
    cin>>n;
    int p;
    cin>>p;
    vector<int>v(p);
    vector<int>g(p);
    for(int i=0;i<p;i++){
        cin>>v[i];
    }
    for(int i=0;i<p;i++){
        cin>>g[p];
    }
    for(int i=0;i<n;i++){
        if(v)
    }
}