#include <iostream>
#include <cmath>
using namespace std;
 int main(){
    int a,b,x;
    cin>>a>>b>>x;
    // int ans = ceil((a+b)/x);
    // cout<<ans;
    //can be imagined but can't code as of now;
 }