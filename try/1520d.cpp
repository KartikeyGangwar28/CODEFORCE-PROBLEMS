#include <iostream>
#include <vector>
using namespace std;
int main(){//can be done by brute force of O(n^2) but search for alternate solution;
int t;
cin>>t;
while (t--)
{
    int n;
    cin>>n;
    vector<int>v(n);
    for(int i=0;i<n;i++){
        cin>>v[i];
    }

}

}